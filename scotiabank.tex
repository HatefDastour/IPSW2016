\begin{frame}
%Yesterday we showed the idea of two approaches,we modified them and got some result.

\begin{frame}{First Approach}
\begin{itemize}
\item $a\in A_1,$ we define a local rank $\overline{R_t^a}$ by
$$\overline{R_t^a}=\sum_{t=1}^Tw_iR_t^a.$$
\item Supose that $(\overline{R_t^a})_{a\in A_1}$ are realizations of the same random variable $R^1$ whose probability density distribution  function is $P_1.$
\item When $a$ varies in $A_1$, we can plot a histogram for $P_1.$%insert the histogram here.
\end{itemize}
\end{frame}

\begin{frame}
\begin{itemize}
\item We then approximate the probability density distribution function $\hat{P_1}$ depending on the data.%insert the pictures and functions
\item We define the function to bend rank in the group 1 into group 2 :
$$ i \longarrow \phi(i),\hspace i={1,2,\dots,17}$$
where \phi is by the formula:
$$\frac{P(R^1\geq D)}{P(R^1 \geq i)}=\frac{P(R^1\geq D)}{P(R^1 \geq i)}$$ 
$$PD^1(x):=PD(R^1=D|R^1=x)=\frac{1-\hat{F_1}(D)}{1-\hat{F_1}(x)}.$$
\item for arbitrary companies a from group 1,b from group 2,we can simply compare $\phi(i)$ and R^2 to decide which one is better. 
\end{itemize}
\end{frame}
